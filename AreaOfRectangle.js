// function that calculates and print area of a rectangle
// length and width given
//area of rectangle = length * width
// print area on console

function areaOfRectangle(length, width) {
  let area = length * width;
  console.log(area);
}
areaOfRectangle(4, 4);
